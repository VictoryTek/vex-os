[Unit]
Description=Bind mount backing store for /nix (optional)
Documentation=man:mount(8)
After=local-fs.target

[Mount]
What=/var/lib/nix
Where=/nix
Type=none
Options=bind

[Install]
WantedBy=local-fs.target
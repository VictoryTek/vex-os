modules:
  # Latest build
  - type: dnf
    repos:
      cleanup: true
      copr:
        - ryanabx/cosmic-epoch
#    install:
#      packages:
#        - cosmic-desktop

    group-install:
      with-optional: true
      packages:
        - cosmic-desktop-environment
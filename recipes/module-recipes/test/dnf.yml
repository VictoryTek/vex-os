---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
modules:
  - type: dnf
    #source: ghcr.io/blue-build/modules/dnf:dnf-module
    repos:
      cleanup: false # clean up added repos after module is done
      files:
        - https://download.docker.com/linux/fedora/docker-ce.repo

      copr:
        - name: faugus/faugus-launcher
          chroot: fedora-43-x86_64
        - name: atim/starship
          chroot: fedora-43-x86_64
        - name: scottames/ghostty
          chroot: fedora-43-x86_64
        - name: peterwu/rendezvous
          chroot: fedora-43-x86_64
        - name: bieszczaders/kernel-cachyos
          chroot: fedora-43-x86_64

      # Put any repo GPG keys here

    # Replace stock kernel with CachyOS kernel
    replace:
      - from-repo: copr:copr.fedorainfracloud.org:bieszczaders:kernel-cachyos
        packages:
          - kernel
          - kernel-core
          - kernel-modules
          - kernel-modules-core
          - kernel-modules-extra

    install:
      packages:
      # System Utilities
        - blivet-gui
        - cockpit
        - fastfetch
        - grsync
        - inxi
        - pavucontrol
        - tailscale
        - tmux

      # GNOME & Theming
        - bibata-cursor-themes                                 # peterwu/rendezvous
        - gnome-tweaks

      # Terminal & Shell Enhancements
        - ghostty                                              # scottames/ghostty
        - starship                                             # atim/starship

      # Gaming Utilities
        - faugus-launcher                                      # faugus/faugus-launcher
        - gamemode

      # Container Tools & Docker
        - buildah
        - docker-ce
        - docker-ce-cli
        - containerd.io
        - docker-buildx-plugin
        - docker-compose-plugin
        - skopeo

      # Dev Tool Packages:
        #- flatpak-builder
        - gtk4-devel
        - libadwaita-devel 
        - glib2-devel 
        - cairo-devel
        - pango-devel 
        - gdk-pixbuf2-devel 
        - rust
        - cargo
        - gcc
        - pkg-config

      # Group installs
    group-install:
      with-optional: true
      packages:
        - virtualization